/**
 * @author mrdoob / http://mrdoob.com/
 */

var WindowBar = function ( editor ) {

	var container = new UI.Panel();
	container.setId( 'windowBar' );
	container.setClass( 'toolbar' );

	// var title = new UI.Panel();
	// title.setId( 'title' );
	// title.setClass( 'windowBarTitle' );
	// title.setTextContent( 'Title' );
	// container.add( title );

	return container;

};
